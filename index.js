// let const declare variables

// update those variables with arithmetic

// logical operators && || 

// control logic (if, else, switch)

// loops (for (#), for of (arrays), while/do while

// get user input (prompt, confirm)


// let item = nums.find(n => n === 20);

// console.log(item)

for (let x = 0; x < 100; x++) {
    console.log(x * 2);
}


// let bigNums = nums.filter(n => n > 10);

// console.log(bigNums)